# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: screenshots.spec.js >> SS1
- Location: tests\screenshots.spec.js:3:5

# Error details

```
Error: locator.textContent: Target page, context or browser has been closed
Call log:
  - waiting for getByRole('button', { name: 'Start' })

```

# Test source

```ts
  1  | import { test } from "@playwright/test"
  2  | 
  3  | test('SS1', async ({ page }) => {
  4  |     await page.goto("https://testautomationpractice.blogspot.com/");
  5  |     await page.getByRole('checkbox', { name: 'Sunday' }).check();
  6  |     await page.screenshot({ path: 'Screenshots/indra.png' });
  7  |     const mondayCheckbox = page.getByRole('checkbox', { name: 'Monday' });
  8  |     await mondayCheckbox.check();
  9  |     await mondayCheckbox.screenshot({ path: 'Screenshots/checkbox.jpeg' });
  10 |     await page.getByRole('radio', { name: 'Male', exact: true }).click();
  11 |     console.log(await page.getByText('Home').first().textContent());
  12 |     await page.getByPlaceholder('Enter Phone').fill('9916910404');
  13 |     const dynamicBtn = page.getByRole('button', { name: 'Start' });
  14 |     await dynamicBtn.click();
> 15 |     console.log(await dynamicBtn.textContent());
     |                                  ^ Error: locator.textContent: Target page, context or browser has been closed
  16 | 
  17 | })
```